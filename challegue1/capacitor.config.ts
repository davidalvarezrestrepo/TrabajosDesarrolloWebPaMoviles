import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dalvarez.miscontactos',
  appName: '\x16Mis Contactos',
  webDir: 'dist'
};

export default config;
